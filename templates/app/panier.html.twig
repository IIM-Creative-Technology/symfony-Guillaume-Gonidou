{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <div class="example-wrapper">
        <h2>Panier </h2>
        <a href="{{ path('app_index') }}">Return to index</a>

            {% for paniers in panier %}
                    <div class="Produit">
                        <h3>{{ paniers.product.name }}</h3>
                        <p> prix: {{ paniers.product.prix }} €</p>
                        <p> nombre de produit en stock {{ paniers.product.stock }}</p>
                        <p> quantiter: {{ paniers.quantiter }} </p>
                        <p> {{ paniers.product.description }}</p>
                        <hr>
                    </div>
                </a>
            {% endfor %}


    </div>
</div>
{% endblock %}
